# Manual QA evidence — v0.1.0-dev.1

> Status: **PENDING** — to be executed by the maintainer against the release
> candidate commit before tagging. This file is the public record the release
> checklist requires; fill in every field and check every row from
> [`docs/qa/manual-regression.md`](../manual-regression.md), then commit it
> BEFORE the final CI run (the tag must point at a SHA that CI has seen).

- Date:
- Machine / OS build:
- Commit under test:
- LM Studio version / model:
- CI Qt platform mode in effect: native windows plugin (see `.github/workflows/ci.yml`)
- Tool versions: uv 0.11.26, build backend uv_build (from `uv.lock`)
- Artifact SHA-256 digests (from the release CI run's package job):

## Results

Copy the checklist sections from `manual-regression.md` here and mark each row
pass/fail with notes. Product signals that are not acceptance failures go to
the issue tracker as follow-ups, not blockers.
